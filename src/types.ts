export type User = {
    name: string,
    hobby: string[],
    age: number,
}

export type dogObject = {
    name: string,
    color: string,
}

export type dogColors = {
    color: string,
    numberOfDogs: number,
}

export type monsterObject = {
    name: string,
    age: number,
    tentacles: number,
    eyes: number,
    hasWings: boolean,
}